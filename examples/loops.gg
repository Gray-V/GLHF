Function find_name()
    load() --only runs once
        count = 0
    end load

    print("Hello world")
    count += 1
end count == 5

for (i = 0, i += 1)
    print("HAI")
end i < array.length

for e in array
    
end for